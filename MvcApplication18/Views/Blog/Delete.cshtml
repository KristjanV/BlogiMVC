@model MvcApplication18.Models.Blog

@{
    ViewBag.Title = "Delete";
       if (!Request.IsAuthenticated)
    {
        Response.Redirect("~/Home/ErrorKiri");
    }
}
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Blog</legend>

    <div class="display-label">pealkiri</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.pealkiri)
    </div>

    <div class="display-label">kuupaev</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.kuupaev)
    </div>

    <div class="display-label">sisu</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.sisu)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
